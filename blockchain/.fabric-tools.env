# Hyperledger Fabric Tools Configuration
# This file contains default configuration for Docker-based fabric tools

# Fabric Tools Docker Image
# Available versions: 2.5, 2.4, latest
FABRIC_TOOLS_IMAGE=hyperledger/fabric-tools:2.5

# User for running containers (to avoid permission issues)
# Default: current user ID and group ID
FABRIC_TOOLS_USER=$(id -u):$(id -g)

# Network Configuration
CHANNEL_NAME=mychannel
CHAINCODE_NAME=mycc
CHAINCODE_VERSION=1.0

# Docker Configuration
COMPOSE_FILE=docker-compose.yaml
NETWORK_NAME=fabric_network

# Advanced Options
# Set to true to pull latest image on every run
FABRIC_TOOLS_ALWAYS_PULL=false

# Set to true for verbose Docker output
FABRIC_TOOLS_VERBOSE=false